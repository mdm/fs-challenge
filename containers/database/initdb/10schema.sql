CREATE TABLE data (
    id INTEGER AUTO_INCREMENT,
    city VARCHAR(255),
    start_date DATE,
    end_date DATE,
    price INTEGER,
    status VARCHAR(255),
    color VARCHAR(7),
    PRIMARY KEY (id)
);
